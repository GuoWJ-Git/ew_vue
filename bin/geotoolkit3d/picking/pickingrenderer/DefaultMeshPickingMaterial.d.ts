import {BasicPickingMaterial} from './BasicPickingMaterial';

/**
 * Material used to do renderer picking on a plain/simple Mesh.<br>
 */
export class DefaultMeshPickingMaterial extends BasicPickingMaterial {
    /**
     * Material used to do renderer picking on a plain/simple Mesh.<br>
     */
    constructor();
}
