import {AbstractShaderPickingMaterial} from './AbstractShaderPickingMaterial';

/**
 * Material used to do renderer picking on a Reservoir mesh.<br>
 */
export class DefaultReservoirPickingMaterial extends AbstractShaderPickingMaterial {
    /**
     * Material used to do renderer picking on a Reservoir mesh.<br>
     * @param options options
     */
    constructor(options: object);
}
