import {AbstractShaderPickingMaterial} from './AbstractShaderPickingMaterial';

/**
 * Material used to do renderer picking on a PointCloud.<br>
 */
export class DefaultPointCloudPickingMaterial extends AbstractShaderPickingMaterial {
    /**
     * Material used to do renderer picking on a PointCloud.<br>
     * @param options options
     */
    constructor(options: object);
}
