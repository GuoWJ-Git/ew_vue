import {AbstractShaderPickingMaterial} from './AbstractShaderPickingMaterial';

/**
 * Material used to do renderer picking on an instanced PointSet.<br>
 */
export class DefaultPointSetPickingMaterial extends AbstractShaderPickingMaterial {
    /**
     * Material used to do renderer picking on an instanced PointSet.<br>
     * @param options options
     */
    constructor(options: object);
}
