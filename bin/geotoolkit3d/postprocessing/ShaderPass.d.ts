import {AbstractPass} from './AbstractPass';

/**
 * This class uses shader program as a renderpass.<br>
 */
export class ShaderPass extends AbstractPass {
    /**
     * This class uses shader program as a renderpass.<br>
     * @param options the ShaderPass options
     */
    constructor(options: object);
}
