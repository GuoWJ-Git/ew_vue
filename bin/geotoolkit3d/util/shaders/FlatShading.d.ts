/**
 * Shader util for computing flat shading on surfaces and contours, require WebGL2 to work (but safe to use with WebGL1).
 */
export class FlatShading {
    /**
     * Shader util for computing flat shading on surfaces and contours, require WebGL2 to work (but safe to use with WebGL1).
     */
    constructor();
}
