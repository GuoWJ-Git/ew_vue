import {AbstractAnnotationLayout} from './AbstractAnnotationLayout';

/**
 * Expanded annotation layout - all annotations locate as vertical line with equal gaps
 */
export class ExpandedAnnotationLayout extends AbstractAnnotationLayout {
    /**
     * Expanded annotation layout - all annotations locate as vertical line with equal gaps
     */
    constructor();
}
