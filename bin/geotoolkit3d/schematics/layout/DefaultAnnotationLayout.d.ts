import {AbstractAnnotationLayout} from './AbstractAnnotationLayout';

/**
 * Default annotation layout - doesn't affect on annotations position
 */
export class DefaultAnnotationLayout extends AbstractAnnotationLayout {
    /**
     * Default annotation layout - doesn't affect on annotations position
     */
    constructor();
}
