import {Point} from '../util/Point';

/**
 * Clip helper class
 */
export class ClipHelper {
    /**
     * Clip helper class
     */
    constructor();
    /**
     * Find a point as intersection of two arrays of points
     * @param pointsPri first array of points
     * @param pointsSec second array of points
     */
    static intersectLines(pointsPri: any[], pointsSec: any[]): Point | null;
}
