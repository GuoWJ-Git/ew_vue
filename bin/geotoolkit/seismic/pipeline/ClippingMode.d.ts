/**
 * Enum of seismic pipeline clipping modes
 */
export enum ClippingMode {
    /**
     * Connected clipping mode
     */
    Connected = 'Connected',
    /**
     * Disconnected clipping mode
     */
    Disconnected = 'Disconnected'
}
