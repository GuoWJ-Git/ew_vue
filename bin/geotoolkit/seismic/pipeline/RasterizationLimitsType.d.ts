/**
 * Enum of seismic pipeline rasterization limits type
 */
export enum RasterizationLimitsType {
    /**
     * Rasterization limits equal normalization limits
     */
    Normalization = 'Normalization',
    /**
     * Rasterization limits equal color map limits
     */
    ColorMap = 'ColorMap'
}
