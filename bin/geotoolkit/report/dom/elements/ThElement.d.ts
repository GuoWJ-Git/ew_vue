import {TdElement} from './TdElement';
import {IElementContainer} from './IElementContainer';

/**
 * Th is a container element
 */
export class ThElement extends TdElement implements IElementContainer {
    /**
     * Th is a container element
     * @param options The options
     */
    constructor(options?: object);
}
