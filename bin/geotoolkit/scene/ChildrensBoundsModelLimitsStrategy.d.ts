import {ModelLimitsStrategy} from './ModelLimitsStrategy';

/**
 * Defines model as union of the model's childrens' bounds
 */
export class ChildrensBoundsModelLimitsStrategy extends ModelLimitsStrategy {
    /**
     * Defines model as union of the model's childrens' bounds
     */
    constructor();
}
