import {AbstractCancellationStrategy} from './AbstractCancellationStrategy';

/**
 * Cancellation strategy, that cancels requests, if they are not in visible model area
 */
export class VisibleAreaCancellationStrategy extends AbstractCancellationStrategy {
    /**
     * Cancellation strategy, that cancels requests, if they are not in visible model area
     */
    constructor();
}
