/**
 * Scaling mode for the Keep aspect ratio
 */
export enum ScalingMode {
    /**
     * Most
     */
    Most = 'most',
    /**
     * Least
     */
    Least = 'least',
    /**
     * Average
     */
    Average = 'average'
}
