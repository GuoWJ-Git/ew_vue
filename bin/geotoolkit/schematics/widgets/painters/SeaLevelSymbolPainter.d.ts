import {SymbolShape} from '../../../scene/shapes/SymbolShape';
import {Rect} from '../../../util/Rect';
import {RenderingContext} from '../../../renderer/RenderingContext';

/**
 * Default sea level drawing
 */
export class SeaLevelSymbolPainter {
    /**
     * Default sea level drawing
     * @param symbol 
     * @param bbox 
     * @param context 
     */
    constructor(symbol: SymbolShape, bbox: Rect, context: RenderingContext);
}
