import {Cancel} from '../util/Cancel';

/**
 * A HttpCancel is an object that can be used to request cancellation of an operation inside of Promise.
 * @deprecated since 2021.1 (3.3) use geotoolkit.util.Cancel instead
 */
export class HttpCancel extends Cancel {
    /**
     * A HttpCancel is an object that can be used to request cancellation of an operation inside of Promise.
     */
    constructor();
}
