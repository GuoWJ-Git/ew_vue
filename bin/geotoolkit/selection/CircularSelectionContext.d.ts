/**
 * Position of point compare with the ellipse
 */
export enum PointPosition {
    /**
     * Point is inside the ellipse
     */
    Inside = 'inside',
    /**
     * Point is outside the ellipse
     */
    Outside = 'outside',
    /**
     * Point is on the ellipse
     */
    On = 'on'
}
