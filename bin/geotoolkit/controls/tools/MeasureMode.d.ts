/**
 * Modes for geotoolkit.controls.tools.Measure tool to determine what is measured
 */
export enum MeasureMode {
    /**
     * Distance measure with polyline
     */
    Distance = 'Distance',
    /**
     * Area measure with polygon
     */
    Area = 'Area'
}
