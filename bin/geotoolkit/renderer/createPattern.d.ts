/**
 * Create a new Pattern URL from a pattern and a specific foreground color
 * @deprecated since 2021.1 (3.3)
 * @param pattern image pattern
 * @param foregroundColor foreground color to be set
 * @param rawSize flag for raw size
 */
export function createPattern(pattern: HTMLImageElement, foregroundColor: string, rawSize: boolean): string;
