import {AbstractProcessor} from './AbstractProcessor';

/**
 * Can process array of number
 */
export class ArrayProcessor extends AbstractProcessor {
    /**
     * Can process array of number
     */
    constructor();
    /**
     * Returns instance of the ArrayProcessor
     */
    static getInstance(): ArrayProcessor;
}
