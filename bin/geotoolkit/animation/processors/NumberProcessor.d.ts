import {AbstractProcessor} from './AbstractProcessor';

/**
 * Can process number
 */
export class NumberProcessor extends AbstractProcessor {
    /**
     * Can process number
     */
    constructor();
    /**
     * Returns instance of the NumberProcessor
     */
    static getInstance(): NumberProcessor;
}
