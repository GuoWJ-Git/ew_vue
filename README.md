**项目使用**

```bash
# Install dependencies
npm install --ignore-scripts

# Repair vite-plugin-mock esbuild mistake
node node_modules/vite-plugin-mock/node_modules/esbuild/install.js

# Start the service
npm start

```
